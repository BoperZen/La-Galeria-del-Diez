@model X.PagedList.IPagedList<La_Galeria_del_Diez.Application.DTOs.AuctionDTO>
@using X.PagedList
@using X.PagedList.Mvc.Core

@{
    ViewData["Title"] = "Draft Auctions";
}

<div class="min-h-screen bg-background py-8">
    <div class="container mx-auto px-4">
        <!-- Header with filters -->
        <div class="mb-8">
            <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-4xl font-bold text-foreground mb-2 flex items-center">
                        <i class="bi bi-file-earmark-text text-primary mr-3"></i>
                        Draft Auctions
                    </h1>
                    <p class="text-muted-foreground">Manage auctions saved as drafts before publishing.</p>
                    @if (ViewBag.Debug != null)
                    {
                        <p class="text-yellow-400 text-sm mt-2">DEBUG: @ViewBag.Debug</p>
                    }
                </div>
                <div class="flex flex-col sm:flex-row gap-3">
                    <a asp-action="Create"
                       class="inline-flex items-center px-6 py-3 bg-primary text-primary-foreground rounded-lg hover:bg-secondary transition-all font-semibold shadow-lg">
                        <i class="bi bi-plus-circle mr-2"></i>
                        Create Auction
                    </a>
                    <a asp-action="Index"
                       class="inline-flex items-center px-6 py-3 bg-muted text-foreground rounded-lg hover:bg-muted/80 transition-all font-semibold">
                        <i class="bi bi-hammer mr-2"></i>
                        View Active
                    </a>
                    <a asp-action="Finalizadas"
                       class="inline-flex items-center px-6 py-3 bg-muted text-foreground rounded-lg hover:bg-muted/80 transition-all font-semibold">
                        <i class="bi bi-clock-history mr-2"></i>
                        View Finished
                    </a>
                </div>
            </div>

            <!-- Filters and search -->
            <div class="bg-card rounded-xl shadow-lg p-6 border border-border">
                <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                    <div class="md:col-span-2">
                        <input type="text"
                               id="searchInput"
                               placeholder="Search by item name..."
                               class="w-full px-4 py-2.5 pl-10 bg-muted border border-border rounded-lg text-foreground placeholder-placeholder focus:outline-none focus:ring-2 focus:ring-ring">
                        <i class="bi bi-search absolute left-3 top-3 text-muted-foreground"></i>
                    </div>
                    <select class="px-4 py-2.5 bg-muted border border-border rounded-lg text-foreground focus:outline-none focus:ring-2 focus:ring-ring">
                        <option>Sort by: Most recent</option>
                        <option>Price: Low to High</option>
                        <option>Price: High to Low</option>
                        <option>Start date</option>
                    </select>
                    <select class="px-4 py-2.5 bg-muted border border-border rounded-lg text-foreground focus:outline-none focus:ring-2 focus:ring-ring">
                        <option>All categories</option>
                        <option>Shirts</option>
                        <option>Balls</option>
                        <option>Medals</option>
                    </select>
                </div>
            </div>
        </div>

        @if (Model.Any())
        {
            <!-- Draft Auctions Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6" id="auctionsGrid">
                @foreach (var auction in Model)
                {
                    var obj = auction.Object;
                    var bidCount = auction.Biddings?.Count ?? 0;
                    var currentPrice = auction.Biddings?.Any() == true
                        ? auction.Biddings.Max(b => b.Amount)
                        : auction.BasePrice;

                    <div class="group bg-card rounded-xl shadow-lg border border-border overflow-hidden hover:shadow-2xl transition-all duration-300 hover:scale-105 auction-card">
                        <!-- Image -->
                        <div class="relative h-64 overflow-hidden bg-muted">
                            @if (obj.Images.Any())
                            {
                                <img src="@Url.Action("GetImage", "Image", new { objectId = obj.Id })"
                                     alt="@obj.Name"
                                     class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300"
                                     loading="lazy">
                            }
                            else
                            {
                                <div class="w-full h-full flex items-center justify-center">
                                    <i class="bi bi-image text-5xl text-muted-foreground"></i>
                                </div>
                            }
                            <!-- Draft badge -->
                            <div class="absolute top-3 right-3">
                                <span class="px-4 py-2 bg-yellow-500/90 text-white text-sm font-bold rounded-full shadow-lg backdrop-blur-sm">
                                    <i class="bi bi-pencil-square mr-1"></i>
                                    Draft
                                </span>
                            </div>

                            <!-- Bids badge -->
                            <div class="absolute top-3 left-3">
                                <span class="px-3 py-1.5 bg-primary/90 text-primary-foreground text-xs font-bold rounded-full shadow-lg backdrop-blur-sm">
                                    <i class="bi bi-people-fill mr-1"></i>
                                    @bidCount bid@(bidCount != 1 ? "s" : "")
                                </span>
                            </div>

                            <!-- Overlay button -->
                            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-6">
                                <a asp-controller="Subasta" asp-action="Details" asp-route-id="@auction.Id"
                                   class="px-6 py-2.5 bg-primary text-primary-foreground rounded-lg hover:bg-secondary transition-all font-semibold shadow-xl transform translate-y-4 group-hover:translate-y-0 transition-transform duration-300">
                                    <i class="bi bi-eye mr-2"></i>
                                    View Details
                                </a>
                            </div>
                        </div>

                        <!-- Content -->
                        <div class="p-5">
                            @if (obj.Categories.Any())
                            {
                                <div class="flex flex-wrap gap-2 mb-3">
                                    @foreach (var cat in obj.Categories.Take(2))
                                    {
                                        <span class="px-2 py-1 bg-accent/10 text-accent text-xs rounded-md border border-accent/20">
                                            @cat.Description
                                        </span>
                                    }
                                </div>
                            }

                            <h3 class="font-bold text-foreground text-lg mb-2 line-clamp-2 min-h-[3.5rem]">
                                @obj.Name
                            </h3>

                            <!-- Info Grid -->
                            <div class="grid grid-cols-2 gap-4 mb-4">
                                <div class="bg-muted/30 rounded-lg p-3">
                                    <p class="text-xs text-muted-foreground mb-1 flex items-center">
                                        <i class="bi bi-calendar-event mr-1"></i>
                                        Start
                                    </p>
                                    <p class="text-sm font-semibold text-foreground">
                                        @auction.StartDate.ToString("dd/MM/yyyy")
                                    </p>
                                </div>

                                <div class="bg-muted/30 rounded-lg p-3">
                                    <p class="text-xs text-muted-foreground mb-1 flex items-center">
                                        <i class="bi bi-currency-dollar mr-1"></i>
                                        Base Price
                                    </p>
                                    <p class="text-sm font-bold text-primary">
                                        $@auction.BasePrice.ToString("N0")
                                    </p>
                                </div>
                            </div>

                            <div class="flex items-center justify-between mb-4 pb-4 border-b border-border">
                                <div>
                                    <p class="text-xs text-muted-foreground mb-1">Current Bid</p>
                                    <p class="text-2xl font-bold text-primary">
                                        $@currentPrice.ToString("N0")
                                    </p>
                                </div>
                            </div>

                            <div class="flex gap-2">
                                <a asp-controller="Subasta" asp-action="Edit" asp-route-id="@auction.Id"
                                   class="flex-1 inline-flex items-center justify-center px-4 py-2.5 bg-primary text-primary-foreground rounded-lg hover:bg-secondary transition-all font-semibold">
                                    <i class="bi bi-pencil-square mr-2"></i>
                                    Edit Draft
                                </a>
                                <form asp-controller="Subasta" asp-action="Publish" asp-route-id="@auction.Id" method="post" class="flex-1">
                                    @Html.AntiForgeryToken()
                                    <button type="submit"
                                            class="w-full inline-flex items-center justify-center px-4 py-2.5 bg-emerald-500 text-white rounded-lg hover:bg-emerald-600 transition-all font-semibold shadow-lg">
                                        <i class="bi bi-rocket-takeoff mr-2"></i>
                                        Publish Auction
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <!-- Empty State -->
            <div class="bg-card rounded-xl shadow-2xl border border-border p-16 text-center">
                <div class="max-w-md mx-auto">
                    <div class="w-24 h-24 bg-muted rounded-full flex items-center justify-center mx-auto mb-6">
                        <i class="bi bi-file-earmark-text text-6xl text-muted-foreground"></i>
                    </div>
                    <h3 class="text-2xl font-bold text-foreground mb-3">No draft auctions yet</h3>
                    <p class="text-muted-foreground mb-6">
                        Draft auctions you save will appear here.
                    </p>
                    <a asp-action="Index"
                       class="inline-flex items-center px-6 py-3 bg-primary text-primary-foreground rounded-lg hover:bg-secondary transition-all font-semibold shadow-lg">
                        <i class="bi bi-hammer mr-2"></i>
                        View Active Auctions
                    </a>
                </div>
            </div>
        }

        @if (Model.PageCount > 1)
        {
            <!-- Pagination -->
            <div class="flex justify-center mt-8">
                <div class="bg-card rounded-xl shadow-lg border border-border p-4">
                    @Html.PagedListPager(Model, page => Url.Action("Borrador", new { page }),
                    new PagedListRenderOptions
                    {
                        LiElementClasses = new string[] { "inline-block" },
                        PageClasses = new string[] { "px-4 py-2 mx-1 rounded-lg bg-muted text-foreground hover:bg-primary hover:text-primary-foreground transition-all" },
                        ActiveLiElementClass = "active",
                        UlElementClasses = new string[] { "flex items-center gap-2" }
                    })
                </div>
            </div>
        }
    </div>
</div>

<script>
    // Live search
    document.getElementById('searchInput')?.addEventListener('input', function(e) {
        const searchTerm = e.target.value.toLowerCase();
        const cards = document.querySelectorAll('.auction-card');

        cards.forEach(card => {
            const text = card.textContent.toLowerCase();
            card.style.display = text.includes(searchTerm) ? '' : 'none';
        });
    });
</script>
